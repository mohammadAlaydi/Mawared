package com.mawared.dawliah.ui.components

import androidx.compose.runtime.Composable

/** Primary CTA button — full-width, PrimaryGreen, with loading state. */
@Composable
fun PrimaryButton(text: String, onClick: () -> Unit, enabled: Boolean = true, isLoading: Boolean = false, modifier: androidx.compose.ui.Modifier = androidx.compose.ui.Modifier) {
    // TODO: Phase 1 — implement primary button with press animation
}

/** Secondary outlined button. */
@Composable
fun SecondaryButton(text: String, onClick: () -> Unit, enabled: Boolean = true, modifier: androidx.compose.ui.Modifier = androidx.compose.ui.Modifier) {
    // TODO: Phase 1 — implement secondary button
}
