package com.mawared.dawliah.ui.components

import androidx.compose.runtime.Composable

/** Reusable top app bar with title, back arrow (RTL-mirrored), and optional actions. */
@Composable
fun MawaredTopBar(title: String, showBack: Boolean = false, onBack: () -> Unit = {}, actions: @Composable () -> Unit = {}) {
    // TODO: Phase 1 — implement styled top bar
}
