package com.mawared.dawliah.ui.components

import androidx.compose.runtime.Composable

/** Section header — title (start) + action text (end). */
@Composable
fun SectionHeader(title: String, actionText: String? = null, onAction: () -> Unit = {}) {
    // TODO: Phase 1 — implement section header row
}
