package com.mawared.dawliah.ui.screens.profile

import androidx.compose.runtime.Composable

/** Profile tab — avatar header, settings menu, account/services/support sections, logout. */
@Composable
fun ProfileScreen(onNavigate: (String) -> Unit, onLogout: () -> Unit) {
    // TODO: Phase 1 — implement full profile menu
}
