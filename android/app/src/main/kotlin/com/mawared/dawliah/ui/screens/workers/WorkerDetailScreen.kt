package com.mawared.dawliah.ui.screens.workers

import androidx.compose.runtime.Composable

/** Worker CV detail — collapsing photo, stats, bio, languages, skills, order button. */
@Composable
fun WorkerDetailScreen(workerId: String, onOrder: () -> Unit, onBack: () -> Unit) {
    // TODO: Phase 1 — implement full worker detail with collapsing header
}
