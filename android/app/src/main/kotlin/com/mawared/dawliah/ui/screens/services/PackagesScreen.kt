package com.mawared.dawliah.ui.screens.services

import androidx.compose.runtime.Composable

/** Service packages screen — hourly/monthly toggle with package cards. */
@Composable
fun PackagesScreen(serviceId: String, onPackageSelected: () -> Unit, onBack: () -> Unit) {
    // TODO: Phase 1 — implement package listing with tab toggle
}
