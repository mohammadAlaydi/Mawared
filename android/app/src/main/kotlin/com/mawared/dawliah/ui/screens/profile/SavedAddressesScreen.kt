package com.mawared.dawliah.ui.screens.profile

import androidx.compose.runtime.Composable

/** Saved addresses — list with edit/delete, add FAB, bottom sheet form. */
@Composable
fun SavedAddressesScreen(onBack: () -> Unit) {
    // TODO: Phase 1 — implement address management
}
