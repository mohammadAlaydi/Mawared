package com.mawared.dawliah.ui.screens.workers

import androidx.compose.runtime.Composable

/** Favorites screen — filtered worker grid + empty state. */
@Composable
fun FavoritesScreen(onWorkerClick: (String) -> Unit, onBack: () -> Unit) {
    // TODO: Phase 1 — implement favorites with empty state
}
