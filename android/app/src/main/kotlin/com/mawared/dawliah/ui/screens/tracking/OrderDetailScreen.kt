package com.mawared.dawliah.ui.screens.tracking

import androidx.compose.runtime.Composable

/** Order detail — status header, details/tracking/support tabs, cancel button. */
@Composable
fun OrderDetailScreen(orderId: String, onBack: () -> Unit) {
    // TODO: Phase 1 — implement order detail with timeline tracking
}
