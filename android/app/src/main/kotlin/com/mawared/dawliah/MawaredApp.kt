package com.mawared.dawliah

import android.app.Application

/**
 * Mawared Al Dawliah Application class.
 * Initializes app-wide dependencies in Phase 1 (DataStore, etc.).
 */
class MawaredApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // TODO: Phase 1 — initialize DataStore for auth persistence
    }
}
