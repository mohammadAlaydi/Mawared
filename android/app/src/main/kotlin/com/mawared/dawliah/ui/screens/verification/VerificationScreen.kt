package com.mawared.dawliah.ui.screens.verification

import androidx.compose.runtime.Composable

/** Signit verification — 4 states: not verified, pending, verified, failed. */
@Composable
fun VerificationScreen(onBack: () -> Unit) {
    // TODO: Phase 1 — implement verification state machine UI
}
