package com.mawared.dawliah.ui.screens.workers

import androidx.compose.runtime.Composable

/** Worker listing — grid with search, filter chips, sort, and filter bottom sheet. */
@Composable
fun WorkersScreen(onWorkerClick: (String) -> Unit, onFavoritesClick: () -> Unit) {
    // TODO: Phase 1 — implement worker grid with filters
}
