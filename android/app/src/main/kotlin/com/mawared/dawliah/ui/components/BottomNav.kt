package com.mawared.dawliah.ui.components

import androidx.compose.runtime.Composable

/** Bottom navigation bar — 4 tabs with animated indicator. */
@Composable
fun MawaredBottomNav(currentRoute: String, onTabSelected: (String) -> Unit) {
    // TODO: Phase 1 — implement Material3 NavigationBar with sliding indicator
}
