package com.mawared.dawliah.ui.components

import androidx.compose.runtime.Composable
import com.mawared.dawliah.data.model.Worker

/** Worker card — photo, name, profession chip, nationality flag, salary, availability. */
@Composable
fun WorkerCard(worker: Worker, isFavorite: Boolean, onFavoriteToggle: () -> Unit, onClick: () -> Unit) {
    // TODO: Phase 1 — implement full worker card design
}
