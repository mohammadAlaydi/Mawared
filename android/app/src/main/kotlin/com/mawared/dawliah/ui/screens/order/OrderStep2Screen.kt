package com.mawared.dawliah.ui.screens.order

import androidx.compose.runtime.Composable

/** Order step 2 — address selection and notes. */
@Composable
fun OrderStep2Screen(onNext: () -> Unit, onBack: () -> Unit) {
    // TODO: Phase 1 — implement address selection step
}
