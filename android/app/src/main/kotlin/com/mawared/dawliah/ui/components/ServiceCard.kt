package com.mawared.dawliah.ui.components

import androidx.compose.runtime.Composable
import com.mawared.dawliah.data.model.ServiceCategory

/** Service category card — icon, title, gradient background. */
@Composable
fun ServiceCard(service: ServiceCategory, onClick: () -> Unit) {
    // TODO: Phase 1 — implement service card with gradient
}
