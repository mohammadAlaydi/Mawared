package com.mawared.dawliah.ui.components

import androidx.compose.runtime.Composable
import com.mawared.dawliah.data.model.ServicePackage

/** Package card — name, duration, price, features checklist, select button. */
@Composable
fun PackageCard(pkg: ServicePackage, isSelected: Boolean, onSelect: () -> Unit) {
    // TODO: Phase 1 — implement package card with popular badge
}
