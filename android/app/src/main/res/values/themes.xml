<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme (used by system before Compose loads) -->
    <style name="Theme.MawaredDawliah" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:colorPrimary">@color/primary_green</item>
        <item name="android:colorPrimaryDark">@color/primary_green_dark</item>
        <item name="android:colorAccent">@color/accent_gold</item>
        <item name="android:windowBackground">@color/background_warm</item>
        <item name="android:statusBarColor">@color/primary_green_dark</item>
        <item name="android:navigationBarColor">@color/surface_white</item>
    </style>

    <!-- Splash screen theme (Android 12+ SplashScreen API) -->
    <style name="Theme.MawaredDawliah.Splash" parent="Theme.MawaredDawliah">
        <item name="android:windowSplashScreenBackground">@color/primary_green</item>
    </style>

</resources>
