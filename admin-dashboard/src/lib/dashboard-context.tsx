// =====================================================================
// DEPRECATED — kept as an empty stub to avoid breaking any cached
// imports during the transition. Every page now reads from TanStack
// Query hooks in src/lib/hooks/use-admin-*.
//
// Safe to delete once you've confirmed no other code paths reference
// `useDashboard()` or `DashboardProvider`.
// =====================================================================

export function useDashboard(): never {
  throw new Error(
    'useDashboard() has been removed. Use the resource hooks under @/lib/hooks instead.',
  );
}
